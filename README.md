# Fee Switch

Minimal Foundry scaffold for the ETEE Protocol fee-splitting settlement task.

Current setup:

- Foundry project initialized
- `forge-std` installed for tests and scripts
- `openzeppelin-contracts` installed for ERC-20 and `SafeERC20`

Useful commands:

```sh
forge build
forge test
forge fmt
```
